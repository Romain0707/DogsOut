<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Snapshot</title>
  <style>
    html, body { margin: 0; padding: 0; background: #f0ede8; }
    #map { width: 800px; height: 450px; }
  </style>
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css">
</head>
<body>
  <div id="map"></div>

  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
  <script>
    /**
     * routeGeoJson est un array PHP (Doctrine type:json).
     * json_encode le convertit en objet JS valide.
     * Peut être une FeatureCollection, un Feature, ou vide [].
     */
    const raw = {{ balade.routeGeoJson is not empty ? balade.routeGeoJson | json_encode | raw : 'null' }};

    const map = L.map('map', {
      zoomControl: false,
      attributionControl: false,
    });

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      maxZoom: 19,
    }).addTo(map);

    // Normalise en FeatureCollection quelle que soit la forme du GeoJSON sauvegardé
    function toFeatureCollection(data) {
      if (!data) return null;
      if (data.type === 'FeatureCollection') return data;
      if (data.type === 'Feature')           return { type: 'FeatureCollection', features: [data] };
      if (data.type === 'LineString' || data.type === 'MultiLineString') {
        return { type: 'FeatureCollection', features: [{ type: 'Feature', geometry: data, properties: {} }] };
      }
      // Array vide [] → null
      if (Array.isArray(data) && data.length === 0) return null;
      return null;
    }

    let ready = false;

    function markReady() {
      if (!ready) { ready = true; window.__SNAPSHOT_READY__ = true; }
    }

    try {
      const geojson = toFeatureCollection(raw);

      if (geojson) {
        const layer = L.geoJSON(geojson, {
          style: () => ({
            color:    '#4a7c59',
            weight:   6,
            opacity:  0.95,
            lineCap:  'round',
            lineJoin: 'round',
          }),
        }).addTo(map);

        const bounds = layer.getBounds();
        if (bounds.isValid()) {
          map.fitBounds(bounds, { padding: [24, 24], maxZoom: 16 });
        } else {
          map.setView([46.2276, 2.2137], 6);
        }
      } else {
        map.setView([46.2276, 2.2137], 6);
      }
    } catch (e) {
      console.error('[snapshot] GeoJSON error:', e);
      map.setView([46.2276, 2.2137], 6);
    }

    // Signal prêt dès que suffisamment de tuiles sont chargées
    let tilesLoaded = 0;
    map.on('tileloadend tileerror', () => {
      tilesLoaded++;
      if (tilesLoaded >= 4) markReady();
    });

    // Fallback 8s si les tuiles sont lentes ou échouent
    setTimeout(markReady, 8000);
  </script>
</body>
</html>
