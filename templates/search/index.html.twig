{% extends 'base.html.twig' %}

{% block title %}Rechercher un utilisateur{% endblock %}

{% block body %}
<section class="search-page">
    <div class="search-page__container">

        <h1 class="search-page__title">Trouver un utilisateur</h1>

        <form class="search-page__form" method="GET" action="{{ path('app_search') }}">
            <div class="search-page__input-wrap">
                <svg class="search-page__icon" width="18" height="18" viewBox="0 0 24 24"
                     fill="none" stroke="currentColor" stroke-width="2"
                     stroke-linecap="round" stroke-linejoin="round">
                    <circle cx="11" cy="11" r="8"></circle>
                    <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
                </svg>
                <input
                    class="search-page__input"
                    type="search"
                    name="q"
                    value="{{ query }}"
                    placeholder="Rechercher par pseudo, prénom ou nom…"
                    autofocus
                >
            </div>
            <button class="search-page__btn" type="submit">Rechercher</button>
        </form>

        {% if query %}
            <p class="search-page__meta">
                {% if results|length > 0 %}
                    {{ results|length }} résultat{{ results|length > 1 ? 's' : '' }} pour
                    <strong>« {{ query }} »</strong>
                {% else %}
                    Aucun résultat pour <strong>« {{ query }} »</strong>
                {% endif %}
            </p>
        {% endif %}

        <div class="search-results">
            {% for user in results %}
            <a href="{{ path('profil_public', {'id': user.id}) }}" class="search-result">
                <div class="search-result__avatar">
                    <img src="{{ user.imageName ? vich_uploader_asset(user, 'imageFile') : asset('profil/default-avatar.png') }}" alt="{{ user.username }}">
                </div>
                <div class="search-result__info">
                    <span class="search-result__username">{{ user.username }}</span>
                    {% if user.firstname or user.lastname %}
                    <span class="search-result__name">
                        {{ user.firstname }} {{ user.lastname }}
                    </span>
                    {% endif %}
                    <span class="search-result__meta">
                        {{ user.chiens|length }} chien{{ user.chiens|length > 1 ? 's' : '' }}
                        · {{ user.balades|length }} balade{{ user.balades|length > 1 ? 's' : '' }}
                    </span>
                </div>
                <span class="search-result__arrow">→</span>
            </a>
            {% endfor %}
        </div>

    </div>
</section>
{% endblock %}
