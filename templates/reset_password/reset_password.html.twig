{% extends 'base.html.twig' %}

{% block title %}Hello ResetPasswordController!{% endblock %}

{% block body %}

<section id="resetpage">
    <div class="container">
        <div class="container-form">
            {{form_start(resetForm)}}
                {{form_widget(resetForm)}}
            {{form_end(resetForm)}}
        </div>
    </div>
</section>

{% endblock %}
